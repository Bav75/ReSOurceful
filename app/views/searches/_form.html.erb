<%# need to add checks for form fields %>
<%# partial for @search - search creation and editing %>
<%= form_for [user, search] do |f| %>
    <%= f.label :tags %>
    <%= f.text_field :tags, multiple: true %>
    <%= f.text_field :tags, multiple: true %>
    <%= f.text_field :tags, multiple: true %>

    <%= f.submit "Enter"%>
<% end %>